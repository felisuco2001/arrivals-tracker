{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "herokuish",
    "buildpackUrl": "heroku/python"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT app:app",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 10
  }
}
